# my-AI-project

Mô tả ngắn: kho chứa mã cho dự án AI cá nhân (chứa nhiều module Python/Node). Đây là repository gốc được tạo từ workspace my-AI-project.

Hướng dẫn nhanh — tạo repo và đẩy lên GitHub:

1. Khởi tạo Git (nếu chưa):

```
git init
git add .
git commit -m "chore: initial commit - add README and .gitignore"
```

2. Tạo remote với GitHub CLI (đã đăng nhập):

```
gh repo create <your-username>/my-AI-project --public --source=. --remote=origin --push --confirm
```

Nếu không dùng gh, tạo repository mới trên GitHub.com rồi liên kết remote và đẩy:

```
git remote add origin https://github.com/<your-username>/my-AI-project.git
git branch -M main
git push -u origin main
```

Nếu bạn muốn tôi tạo repository remote tự động (sử dụng `gh`), cho tôi biết và tôi sẽ chạy lệnh đó nếu bạn đã cấp quyền/đăng nhập trên máy này.

---
Generated by assistant.
